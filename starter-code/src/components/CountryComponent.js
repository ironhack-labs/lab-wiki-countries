import React from "react";

const CountryComponent = ({ name, flag }) => {
  return (
    <div className="container">
      <h2>{name}</h2>
      <p>{name}</p>
    </div>
  );
};

export default CountryComponent;
