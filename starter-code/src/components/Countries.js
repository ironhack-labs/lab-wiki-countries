import React from 'react'

const Countries = (props) => {
  return (
    <div>
      <p>Data</p>
      {console.log(props.data)}
      <p>{this.props.data}</p>
    </div>
  )
}

export default Countries
