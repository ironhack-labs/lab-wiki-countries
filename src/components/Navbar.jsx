import { NavLink } from "react-router-dom";

function Navbar() {
    return (
        <div>
            <NavLink to="/">
                WikiCountries
            </NavLink>
        </div>
    );
}

export default Navbar;
