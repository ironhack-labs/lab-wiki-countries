import { NavLink } from "react-router-dom";

const Navbar =() => {
    return (
        <nav className="Navbar">
                <NavLink to="/" className='NavBar'>LAB - WikiCountries</NavLink>
        </nav>
    )
}

export default Navbar; 