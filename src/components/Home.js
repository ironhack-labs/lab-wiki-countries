import React from "react";

const Home = ({ name }) => {
  return (
    <div>
      <h1> Hello {name}</h1>
      <p> Select the couuntry to see the archive</p>
    </div>
  );
};

export default Home;