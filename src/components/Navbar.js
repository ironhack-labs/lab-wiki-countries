import { Link , NavLink} from 'react-router-dom';

function Navbar() {
  return (
    <nav class="navbar navbar-dark bg-primary mb-3">
      <div class="container">
        <h3 class="navbar-brand"> WikiCountries </h3>
      </div>
    </nav>
  );
}

export default Navbar;
