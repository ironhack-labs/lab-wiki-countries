
import React, { Component } from "react";

const Navbar = props => {
    return <nav class="navbar navbar-dark bg-dark mb-3">
        <div class="container">
            <h1>WikiCountries</h1>
        </div>
    </nav>
}

export default Navbar;